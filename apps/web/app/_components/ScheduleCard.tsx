import { css } from "../../styled-system/css";

export function ScheduleCard() {
  return (
    <div
      className={css({
        width: "100%",
        p: "12px",
        bg: "white",
        height: "105px",
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "center",
      })}
    >
      ScheduleCard
    </div>
  );
}
