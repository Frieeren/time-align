export enum ScheduleParticipantRole {
  ORGANIZER = "ORGANIZER",
  ATTENDEE = "ATTENDEE",
}

export enum ScheduleParticipantResponseStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  DECLINED = "DECLINED",
  TENTATIVE = "TENTATIVE",
}
